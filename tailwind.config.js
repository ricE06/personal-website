// tailwind.config.js
import defaultTheme from 'tailwindcss/defaultTheme';

export default {
  theme: {
    extend: {
      fontFamily: {
        // monocustom: ['CustomMono', ...defaultTheme.fontFamily.monospace], 
      },
    },
  },
  plugins: [],
};
